@model IEnumerable<MedAgenda.WebMVC.Models.MedicalSpecialtyDto>

@foreach (var item in Model)
{
    <tr>
        <td>@item.Id</td>
        <td>@item.Name</td>
        @* Quitamos la descripción para que no dé error *@
    </tr>
}